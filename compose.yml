services:
  soju:
    restart: ${RESTART_POLICY:-no}
    container_name: soju
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - 6667:6667
    volumes:
      - ./soju/:/var/lib/soju/
      - ./config.ini:/etc/soju/config.ini:ro
